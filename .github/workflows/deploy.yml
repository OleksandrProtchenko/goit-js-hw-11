name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: useApiKey 🛎️
        shell: bash
        env:
          MY_SECRET_KEY: ${{ secrets.VITE_PIXABEY_KEY }}
          MY_VAR: ${{ vars.VITE_PIXABEY_KEY }}
        run: echo "Сервіс $MY_VAR запущено"

      - name: Install and build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: dist
